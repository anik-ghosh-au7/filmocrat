import React from 'react';
import { Card } from 'antd';

const Scripts = () => {
	return (
		<Card
			style={{
				display: 'flex',
				height: '100%',
				width: '100%',
				justifyContent: 'center',
				alignItems: 'center',
			}}
		>
			Scripts
		</Card>
	);
};

export default Scripts;
